# Fresh Start Mobile Detailing

Website source. Hosted on GitHub Pages.

## Deploy
Pages is set to deploy from the `main` branch, `/ (root)` folder.
Push to `main` and it auto-updates — no build step needed for plain HTML/CSS/JS.

Live at: https://cpeleck.github.io/freshstart-detailing/
(or your custom domain once DNS is pointed here)
